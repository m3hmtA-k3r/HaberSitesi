@using Shared.Dtos
@model List<BlogKategorilerDto>

@{
    var aktifSayisi = Model.Count(x => x.AktifMi == true);
    var pasifSayisi = Model.Count(x => x.AktifMi == false);
    var toplamSayisi = Model.Count;
    var toplamBlog = Model.Sum(x => x.BlogSayisi);
}

<style>
    .stats-card { border-radius: 12px; padding: 1.5rem; color: #fff; transition: transform 0.2s ease, box-shadow 0.2s ease; }
    .stats-card:hover { transform: translateY(-3px); box-shadow: 0 8px 25px rgba(0,0,0,0.15); }
    .stats-card .stats-icon { width: 50px; height: 50px; border-radius: 10px; display: flex; align-items: center; justify-content: center; font-size: 1.5rem; background: rgba(255,255,255,0.2); }
    .stats-card .stats-number { font-size: 2rem; font-weight: 700; }
    .stats-card .stats-label { font-size: 0.85rem; opacity: 0.9; }
    .stats-total { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); }
    .stats-active { background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%); }
    .stats-passive { background: linear-gradient(135deg, #eb3349 0%, #f45c43 100%); }
    .stats-blogs { background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%); }

    .data-table th { background: #f8f9fa; font-weight: 600; font-size: 0.85rem; text-transform: uppercase; letter-spacing: 0.5px; color: #495057; border-bottom: 2px solid #dee2e6; }
    .data-table td { vertical-align: middle; font-size: 0.9rem; }
    .badge-status { padding: 0.4rem 0.8rem; border-radius: 20px; font-weight: 500; font-size: 0.75rem; }
    .badge-active { background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%); color: #fff; }
    .badge-passive { background: linear-gradient(135deg, #eb3349 0%, #f45c43 100%); color: #fff; }

    .btn-action { width: 38px; height: 38px; padding: 0; display: inline-flex; align-items: center; justify-content: center; border-radius: 8px; transition: all 0.2s ease; }
    .btn-action:hover { transform: scale(1.1); }
    .btn-add { background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%); border: none; color: #fff; padding: 0.6rem 1.2rem; border-radius: 8px; font-weight: 500; transition: all 0.2s ease; }
    .btn-add:hover { transform: translateY(-2px); box-shadow: 0 4px 15px rgba(17, 153, 142, 0.4); color: #fff; }
    .btn-bulk-delete { background: linear-gradient(135deg, #eb3349 0%, #f45c43 100%); border: none; color: #fff; padding: 0.5rem 1rem; border-radius: 8px; font-weight: 500; display: none; }
    .btn-bulk-delete.show { display: inline-flex; align-items: center; gap: 0.5rem; }

    .form-check-input { width: 18px; height: 18px; cursor: pointer; }
    .form-check-input:checked { background-color: #667eea; border-color: #667eea; }
    .card-header-custom { background: #fff; border-bottom: 1px solid #e2e8f0; padding: 1rem 1.5rem; }
    .card-header-custom h5 { margin: 0; font-weight: 600; color: #2d3748; }

    .modal-confirm .modal-header { border-bottom: none; padding: 1.5rem 1.5rem 0; }
    .modal-confirm .modal-body { text-align: center; padding: 1.5rem; }
    .modal-confirm .modal-footer { border-top: none; justify-content: center; padding: 0 1.5rem 1.5rem; gap: 0.5rem; }
    .modal-confirm .icon-box { width: 80px; height: 80px; border-radius: 50%; margin: 0 auto 1.5rem; display: flex; align-items: center; justify-content: center; background: #fff3f3; }
    .modal-confirm .icon-box i { font-size: 2.5rem; color: #ef4444; }

    @@media (max-width: 768px) {
        .mobile-card { display: block; }
        .desktop-table { display: none; }
        .item-card { background: #fff; border-radius: 12px; padding: 1rem; margin-bottom: 1rem; box-shadow: 0 2px 8px rgba(0,0,0,0.08); border: 1px solid #e2e8f0; }
    }
    @@media (min-width: 769px) {
        .mobile-card { display: none; }
        .desktop-table { display: block; }
    }
</style>

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h3 class="mb-1">Blog Kategori Yonetimi</h3>
            <p class="text-muted mb-0">Blog kategorilerini goruntuleyin ve yonetin</p>
        </div>
        <a href="/BlogKategori/Ekle" class="btn btn-add"><i class="fa fa-plus me-2"></i> Yeni Kategori</a>
    </div>

    <div class="row mb-4">
        <div class="col-6 col-lg-3 mb-3 mb-lg-0">
            <div class="stats-card stats-total">
                <div class="d-flex justify-content-between align-items-center">
                    <div><div class="stats-number">@toplamSayisi</div><div class="stats-label">Toplam Kategori</div></div>
                    <div class="stats-icon"><i class="fas fa-folder-open"></i></div>
                </div>
            </div>
        </div>
        <div class="col-6 col-lg-3 mb-3 mb-lg-0">
            <div class="stats-card stats-active">
                <div class="d-flex justify-content-between align-items-center">
                    <div><div class="stats-number">@aktifSayisi</div><div class="stats-label">Aktif Kategori</div></div>
                    <div class="stats-icon"><i class="fas fa-check-circle"></i></div>
                </div>
            </div>
        </div>
        <div class="col-6 col-lg-3">
            <div class="stats-card stats-passive">
                <div class="d-flex justify-content-between align-items-center">
                    <div><div class="stats-number">@pasifSayisi</div><div class="stats-label">Pasif Kategori</div></div>
                    <div class="stats-icon"><i class="fas fa-pause-circle"></i></div>
                </div>
            </div>
        </div>
        <div class="col-6 col-lg-3">
            <div class="stats-card stats-blogs">
                <div class="d-flex justify-content-between align-items-center">
                    <div><div class="stats-number">@toplamBlog</div><div class="stats-label">Toplam Blog</div></div>
                    <div class="stats-icon"><i class="fas fa-blog"></i></div>
                </div>
            </div>
        </div>
    </div>

    <div class="card border-0 shadow-sm">
        <div class="card-header-custom d-flex justify-content-between align-items-center">
            <h5><i class="fas fa-list me-2 text-primary"></i>Kategori Listesi</h5>
            <button type="button" class="btn btn-bulk-delete" id="bulkDeleteBtn" onclick="bulkDelete()">
                <i class="fas fa-trash-alt"></i> <span id="selectedCount">0</span> Secili Sil
            </button>
        </div>
        <div class="card-body">
            <div class="desktop-table">
                <table class="table table-hover data-table" id="dataTables-example" width="100%">
                    <thead>
                        <tr>
                            <th style="width: 40px;"><input type="checkbox" class="form-check-input" id="selectAll" onchange="toggleSelectAll()"></th>
                            <th>Sira</th>
                            <th>Kategori Adi</th>
                            <th>Aciklama</th>
                            <th>Blog Sayisi</th>
                            <th>Durum</th>
                            <th style="width: 100px;">Islemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.OrderBy(x => x.Sira))
                        {
                            <tr>
                                <td><input type="checkbox" class="form-check-input row-checkbox" value="@item.Id" onchange="updateBulkButton()"></td>
                                <td><span class="badge bg-secondary">@item.Sira</span></td>
                                <td><strong>@item.Adi</strong></td>
                                <td><span class="text-muted">@(item.Aciklama ?? "-")</span></td>
                                <td><span class="badge bg-primary">@item.BlogSayisi blog</span></td>
                                <td>
                                    @if (item.AktifMi) {<span class="badge-status badge-active">Aktif</span>}
                                    else {<span class="badge-status badge-passive">Pasif</span>}
                                </td>
                                <td>
                                    <div class="d-flex gap-1">
                                        <a href="/BlogKategori/Guncelle?kategoriId=@item.Id" class="btn btn-primary btn-action" title="Duzenle"><i class="fa fa-edit"></i></a>
                                        <button type="button" class="btn btn-danger btn-action" onclick="confirmDelete(@item.Id, '@item.Adi?.Replace("'", "\\'")?.Replace("\"", "&quot;")')"><i class="fa fa-trash-alt"></i></button>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            <div class="mobile-card">
                @foreach (var item in Model.OrderBy(x => x.Sira))
                {
                    <div class="item-card">
                        <div class="d-flex justify-content-between align-items-start mb-2">
                            <div class="d-flex align-items-center gap-2">
                                <input type="checkbox" class="form-check-input row-checkbox" value="@item.Id" onchange="updateBulkButton()">
                                <div>
                                    <strong>@item.Adi</strong>
                                    <div class="text-muted small">@(item.Aciklama ?? "-")</div>
                                </div>
                            </div>
                            <span class="badge bg-secondary">Sira: @item.Sira</span>
                        </div>
                        <div class="d-flex justify-content-between align-items-center mt-3 pt-3 border-top">
                            <div class="d-flex gap-2">
                                <span class="badge bg-primary">@item.BlogSayisi blog</span>
                                @if (item.AktifMi) {<span class="badge-status badge-active">Aktif</span>}
                                else {<span class="badge-status badge-passive">Pasif</span>}
                            </div>
                            <div class="d-flex gap-1">
                                <a href="/BlogKategori/Guncelle?kategoriId=@item.Id" class="btn btn-primary btn-action"><i class="fa fa-edit"></i></a>
                                <button type="button" class="btn btn-danger btn-action" onclick="confirmDelete(@item.Id, '@item.Adi?.Replace("'", "\\'")?.Replace("\"", "&quot;")')"><i class="fa fa-trash-alt"></i></button>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<div class="modal fade modal-confirm" id="deleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header"><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div>
            <div class="modal-body">
                <div class="icon-box"><i class="fas fa-exclamation-triangle"></i></div>
                <h4 class="mb-3">Silmek istediginize emin misiniz?</h4>
                <p class="text-muted mb-0" id="deleteMessage">Bu kategoriyi silmek uzeresiniz.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary px-4" data-bs-dismiss="modal">Iptal</button>
                <a href="#" class="btn btn-danger px-4" id="confirmDeleteBtn"><i class="fas fa-trash-alt me-1"></i> Sil</a>
            </div>
        </div>
    </div>
</div>

<div class="modal fade modal-confirm" id="bulkDeleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header"><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div>
            <div class="modal-body">
                <div class="icon-box"><i class="fas fa-exclamation-triangle"></i></div>
                <h4 class="mb-3">Toplu silme islemi</h4>
                <p class="text-muted mb-0"><span id="bulkDeleteCount">0</span> kategori silinecek.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary px-4" data-bs-dismiss="modal">Iptal</button>
                <button type="button" class="btn btn-danger px-4" onclick="executeBulkDelete()"><i class="fas fa-trash-alt me-1"></i> Tumunu Sil</button>
            </div>
        </div>
    </div>
</div>

<script>
    function confirmDelete(id, title) {
        document.getElementById('deleteMessage').innerHTML = '<strong>"' + title + '"</strong> kategorisini silmek uzeresiniz.<br><small class="text-muted">Bu islem geri alinamaz.</small>';
        document.getElementById('confirmDeleteBtn').href = '/BlogKategori/Sil?kategoriId=' + id;
        new bootstrap.Modal(document.getElementById('deleteModal')).show();
    }
    function toggleSelectAll() {
        var selectAll = document.getElementById('selectAll');
        document.querySelectorAll('.row-checkbox').forEach(cb => cb.checked = selectAll.checked);
        updateBulkButton();
    }
    function updateBulkButton() {
        var checked = document.querySelectorAll('.row-checkbox:checked').length;
        document.getElementById('selectedCount').textContent = checked;
        document.getElementById('bulkDeleteBtn').classList.toggle('show', checked > 0);
        var all = document.querySelectorAll('.row-checkbox').length;
        document.getElementById('selectAll').checked = checked === all && all > 0;
    }
    function bulkDelete() {
        document.getElementById('bulkDeleteCount').textContent = document.querySelectorAll('.row-checkbox:checked').length;
        new bootstrap.Modal(document.getElementById('bulkDeleteModal')).show();
    }
    function executeBulkDelete() {
        var ids = Array.from(document.querySelectorAll('.row-checkbox:checked')).map(cb => cb.value);
        if (ids.length > 0) {
            Promise.all(ids.map(id => fetch('/BlogKategori/SilAjax?kategoriId=' + id, { method: 'POST' }))).then(() => location.reload());
        }
        bootstrap.Modal.getInstance(document.getElementById('bulkDeleteModal')).hide();
    }
</script>
